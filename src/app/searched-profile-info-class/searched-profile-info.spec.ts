import { SearchedProfileInfo } from './searched-profile-info';

describe('SearchedProfileInfo', () => {
  it('should create an instance', () => {
    expect(new SearchedProfileInfo()).toBeTruthy();
  });
});
