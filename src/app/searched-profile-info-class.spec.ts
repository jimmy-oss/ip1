import { SearchedProfileInfoClass } from './searched-profile-info-class';

describe('SearchedProfileInfoClass', () => {
  it('should create an instance', () => {
    expect(new SearchedProfileInfoClass()).toBeTruthy();
  });
});
